# Copilot / 通用 Agent 指令

以下文件包含本仓库的全部 Agent 工作约定，请优先阅读：

1. `AGENTS.md` —— 项目说明、铁律、多 Agent 协作流程（唯一入口）
2. `docs/handoff/HANDOFF.md` —— 当前状态、已验证事实、阻塞点、下一步
3. `docs/handoff/TASKS.yaml` —— 任务板（可认领）

核心约束：不许假装成功、尊重平台真实学习时间、任务中心按分组顺序解锁、
面向同学的文本必须去 AI 味、改代码必须补离线单测。
