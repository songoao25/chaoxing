# 决策记录（ADR-lite）

> 想推翻某条做法之前，先看这里有没有已经决策过；要改就新增一条，不要悄悄改。

| ID | 决策 | 原因 | 日期 |
| --- | --- | --- | --- |
| D1 | 任务中心与章节**分开编排**，章节刷完后再跑任务中心 | 两者数据源和行为都不同；章节并发快，任务中心是串行闯关 | 2026-09-16 |
| D2 | 任务中心整段**失败软着陆**，绝不影响章节刷课 | 章节是主链路，不能因为新功能把老功能弄挂 | 2026-09-16 |
| D3 | 有 `enableVideoWatchDuration` 的视频**强制 1 倍速**，不够就回看 | 实测 2 倍速只计一半、原地心跳不计；只有"播完"类才用配置倍速 | 2026-09-16 |
| D4 | 不支持/失败的任务点**返回失败并写日志**，绝不假装完成 | 历史 issue #223/#357：把"没做"记成"完成"会误导用户 | 2026-09-16 |
| D5 | 文档完成走 `readPoint` 打点 + `readEnd`（当前未打通，见任务 A） | 这是阅读器真实行为；在拿到 enc 规律前不引入更 hack 的手段 | 2026-09-16 |
| D6 | 面向老师/同学的文本统一走 `api/ai_writer.py` | 用户明确要求去 AI 味，讨论要模仿真实同学 | 2026-09-16 |
| D7 | Agent 协作以 `AGENTS.md + `docs/handoff/TASKS.yaml` 为准 | 多 Agent 接手需要机器可读的任务板与唯一入口 | 2026-09-16 |
| D8 | 探针脚本入库到 `tools/probe/`，路径改成相对路径 | 交接后可直接复现实验，不依赖上一位 Agent 的临时文件 | 2026-09-16 |
| D9 | 新增功能必须带**离线单测**（FakeSession/FakeTC） | 平台接口随时变，离线测试保证重构安全 | 2026-09-16 |
| D10 | 任务中心会改变课程记录的提交默认 `confirm`，只有显式 `auto` 才自动提交；完成必须同时满足接口业务成功、分数（如有）和任务状态复查 | 避免 `--yes` 或非交互运行误提交，也避免把 HTTP 200、等待或跳过误报为完成 | 2026-09-16 |
| D11 | 章节类任务点的同步**只认平台下发的 `stuJobInfo`**（视频打点 / `/mooc-ans/job/document` 响应里），CLI 不得自己拼 `enc`；拿不到就报"未同步"并等复查 | 网页端就是这么做的；前几轮自己猜 `enc` 全部被服务端以 `enc不正确` 拒绝 | 2026-09-17 |
| D12 | mooc 视频打点在部分网络环境下会被风控按**客户端指纹**拦截（Python requests/OpenSSL 403，curl/浏览器 200），CLI 在 requests 收到 403 时用系统 curl **原样重放一次**（cookie 走 stdin），重放成功按正常响应处理 | 实测同一 URL/参数/Cookie/UA：Chrome 200、curl 200、requests 403；不处理的话章节视频任务点永远刷不动 | 2026-09-17 |
| D13 | 视频进度已到结尾（`playTime >= duration`）但平台未通过时，**从头回看一遍**，不再在结尾反复重报 | 平台记的是"看到的位置"，通过看的是累计观看时长；实测浏览器从 0 播到 ~232s 才被通过，而在结尾连报 30 次也不会通过（#358/#451 的根因） | 2026-09-17 |
| D14 | 配置 `endpoint=api.deepseek.com` + `model=deepseek-flash` 时，**所有**大模型调用都要显式关闭 thinking（`thinking={"type":"disabled"}`）；`ai_writer` 也必须带，拿不到正文就重试一次并报错，绝不返回空文本 | deepseek-flash 默认开 thinking，`message.content` 为空；`api/answer.py` 早有这个处理，但 `api/ai_writer.py` 漏了，导致 AI实践/讨论生成的回答实测为空字符串（2026-09-17 真实调用复现） | 2026-09-17 |
| D15 | AI实践的完成判定必须**同时**满足"平台没有 pending 题"和状态完成，提交后要等平台评估（`answerRecords` 出现该 `recordUuid` 且 `statusMsg=已评估`）才算拿到分数 | 实测：还有 pending 题就提交，评估出来只有 60 分；页面写明达标线是"学生多次练习平均分达到 60 分"，分数来自平台对每一次练习的评估 | 2026-09-17 |
| D16 | AI实践的判分是**平台自己的大模型**，同一题同一个答案会被反复判错：实现上按题干记住已试过的答案，重试时换一个选项；平台反馈只作为模型内部判据 | 实测同一道单选连发 6 次同答案全部判错；换一个选项后立刻"回答正确"并推进到下一个知识点 | 2026-09-17 |
| D17 | **选择题/判断题只提交字母或"对/错"**；平台反馈（preAppendContent/`_recent_feedback`）绝不能进入任何会提交的正文；正文里出现平台判分话术一律重写，重写不干净直接报错不提交 | 用户实测发现选择题被回成"选 D。……依据：回答正确！本题考核知识点为……"，既是错误作答格式，也是自曝 AI | 2026-09-17 |
| D18 | 面向平台提交的文本要过**两遍独立真人化审计**（自动审计器只是下限，第二遍由独立上下文的 Agent 复核，可推翻第一遍）；"每篇都金句收尾""无来源的假具体""口癖均匀撒"列为硬伤 | 两轮独立审计：4 篇里只有 1 篇过；选代 2 被判"明显不是真人"并揪出事实错误；审计器本身也被指出只认关键词、可被同义词绕过 | 2026-09-17 |
| D19 | AI实践的 `answer/submit` **只是结束作答，不会出分**：提交后必须请求一次 `/ai-ans/ai-evaluate/think/end-report`（页面上是"学习质量评估报告"按钮），成绩才会被现算并回填 `answerRecords`；拿不到分数就不算完成 | 实测提交后轮询 10 分钟 `answerRecords` 里都没有这条记录、`scoreCount` 不涨；调一次 end-report 立刻返回 `{"id":"score","content":"100"}` 并回填 | 2026-09-17 |
| D20 | AI实践的完成判定**两条口径一起看**：本次成绩与**练习平均分**都要到目标线（`ai_practice_min_score`，默认 85）；本轮已达标但历史低分把平均分拖下来时，只要还有重答次数就继续补答。次数用尽后：单次成绩没到线 → 返回失败；**单次已到线、只是平均分没到** → 记"完成"并打印明确告警（平台侧 `isFinish` 仍由上层复查兜底），不空转 | 页面写的达标标准是"学生多次练习**平均分**达到 N 分"，历史 30/60 分的记录会一直拖后腿；平均分是本地质量目标，用尽次数后谎报"未完成"会把平台已完成的任务点说成失败 | 2026-09-17 |
| D21 | AI实践的"最后一题被平台推回"必须**限次收手**：同一道题最多试满一轮选项（4 次），且 `unCompleteTopic` 为空时连续两次作答没有让 `messageList` 变长就直接提交 | 实测知识点全答完后平台仍把最后一道题推回来、又不收录作答，本地"错/对"来回换了十几次（历史上整局拖到 112 题、空转 23 分钟） | 2026-09-17 |
| D22 | 提交确认提示（`input()`）必须先让键盘监听线程让出 stdin：`api/interrupt.py` 增加 `pause_watcher()` / `stdin_for_prompt()`，`confirm_submission` 用它包住 `input()` | 独立审计用 pty 复现：章节阶段起的监听线程把终端设成 cbreak 并一直 `os.read(stdin,1)`，与 `input()` 抢字符——逐字敲 "yes" 5/5 失败（4 次被吃成 "ye"/"ys" 当取消、1 次永久挂住），且用户看不到回显 | 2026-09-17 |
| D23 | 成绩**只认本条记录**：`_ai_score(data, recordUuid)` 在本条还没评估时返回 None，绝不回落到顶层 `answerScore`（那是上一次练习的分）；`end-report` 一旦给出成绩就直接采用，只再读一次状态，不再固定轮询 19 次×30s | 独立审计：旧实现会把上一次练习的分数当成本次成绩；报告已经出分后仍空等约 10 分钟，5 轮最坏 ~47 分钟且不响应退出键 | 2026-09-17 |
| D24 | 同一任务点在**本次运行内失败后不再重试**（`failed_plans`），"等解锁"的轮次只为解锁服务；任务中心整段再包一层 try/except 软着陆 | 独立审计：混合分组里一个失败的 AI实践会被重试到 `TASK_CENTER_MAX_ROUNDS=6` 次（每次好几分钟）；且未预期异常会跳过其后的统计与学习次数 | 2026-09-17 |
| D25 | 题型编码认不出来（如 `questionTypeInt="2"`）但题目带选项时，一律按**客观题**作答；只有确实没有选项才走简答 | 独立审计复现：非空但未识别的题型编码会让选择题被写成一整段小作文（平台判"没有按题目要求作答"） | 2026-09-17 |
| D26 | 向导（`cx`）登录后必须**显式问一次**刷课范围（章节+任务中心 / 只刷章节 / 只刷任务中心），结果写进本次账号配置；新增 `chapter_study` 配置与 `--chapters/--no-chapters`，且两个开关不能同时为 false | 章节和任务中心是两套独立入口，旧向导从不询问、直接吃全局默认 `task_center=true`，用户登录后找不到"只刷章节/只刷任务"的入口；顺带修掉 `cx --yes` 没有真的跳过确认、也没透传 `--yes` 的问题 | 2026-09-17 |
| D27 | 任务中心也支持"每门课刷前 N 个教学任务"（`max_tasks_per_course` / `--max-tasks` / 向导逐门询问），语法与 `max_points_per_course` 相同；被限制没处理的任务计入 `limited`，**不算失败** | 用户反馈"只刷任务中心时默认全部任务，没有像章节那样的数量选择"；把"没刷"记成失败会误导下次运行 | 2026-09-20 |
| D28 | 任务中心作业（planType=4）走**新版学习页 + `addStudentWorkNewWeb`**：`GET mooc2/work/task`（302 到 `dowork`）→ 解析 `form#submitForm` → 提交 `action + &pyFlag=&ua=pc&formType=post&saveStatus=1&version=1`；**不复用**章节测验的 `mooc-ans/api/work`（同一 session 下对课程级作业实测 403）。题目解析新增 `decode_homework_page`（题型在 `answertype<id>`，题干要按 DOM 顺序拼——真实页面把 `<p>` 嵌在 `<h3>` 里，lxml 会提前闭合 h3）；客观题走题库、简答题走 ai_writer，提交前走 confirm 门禁，完成只认任务中心 `isFinish` | 2026-09-20 真机取证：作业页 200 且题目/表单齐全，而 `api/work` 对同一作业 403；只取 h3 会丢题干（实测第 1 题和填空题全丢，AI 只能瞎答） | 2026-09-20 |
| D29 | 任务中心提交模式**默认 `auto`**（作业/AI实践/讨论作答完自动提交），`confirm` 改为需要时才显式配置；向导不再询问刷课参数（倍速/并发/未开放/重做/提交模式），直接用推荐值并在启动时只显示一行 | 用户真机反馈：挂后台刷课时每条作业都要回车确认，不可用；"具体配置不用再问，直接按最优推荐，仅启动时显示一遍" | 2026-09-20 |
| D30 | 控制台噪声治理：**刷课全程（含任务中心阶段）控制台静音**，内部 INFO/DEBUG（任务点读取、逐页 mArg、题库明细、限流等待）只进日志文件；`knowledge/cards` 逐页"找不到 mArg"不再逐页打日志，每章汇总一条 DEBUG；暂不支持的任务点同轮不重试 | 用户真机日志里同一章 6 条"找不到 mArg"、每道题的"原始标题/处理后标题"全部刷在控制台，非技术用户看不懂也误以为是错误 | 2026-09-20 |
| D31 | 主题讨论（planType=14）走 groupweb 真实接口：`getReplyList` 读已有回复参考风格、`addReplys` 提交（`replyId=-1`、`topic_content` 先 encodeURIComponent 再被表单编码一次、urlToken 每次提交后换新）；回复文本必须过 `HumanLikeWriter.discussion`，生成器判定"编造个人经历/平台话术"等硬伤时**直接放弃提交**；同一话题自己已回复过则**不再重复发帖** | 2026-09-20 真机取证：讨论区、章节、作业、任务引擎是四套不同接口；实测模型会编造"我在奶茶店做过兼职"、会引用不存在的"第七条"，因此把编造履历升级为硬失败、并禁止引用具体楼层 | 2026-09-20 |
| D33 | 主题讨论回复的风格目标是**"班里中等水平"**：长度与多数同学接近（约 110~180 字）、直接给观点 + 一两条课本/常识性理由、平实不显眼；明确禁止巧妙类比、抖机灵、金句/排比、刻意示弱、800 字小论文。实现上改 `HumanLikeWriter.discussion` 的提示词与目标长度（`max_chars=180`），并取消强制的"示弱/口语碎片"要求 | 用户实测反馈：上一版"要我说…我甚至觉得背下来也没啥用"太机灵、太有个人风格，反而不像普通同学；要求"不要写得太好也不要写得太差，必须普普通通" | 2026-09-20 |
| D32 | 任务引擎对讨论的完成同步**有几分钟延迟**：真机回复成功且读回一致，22:28 复查 `isFinish=false`、22:31 复查 `isFinish=true`。策略：**回复只发一次、不重复刷屏、不假装完成**，完成与否继续只认 `getPlanDataByGroupId.isFinish`（延迟由上游的 wait/复查兜住） | 接口提交成功不等于任务点完成（铁律 1）；同时重复运行不能靠反复发帖"试出来" | 2026-09-20 |
| D34 | DeepSeek 答题链路：thinking 默认 **auto**（不再写死 disabled，V4.1 flash 默认带推理，正确率更高；正文为空时自动降级 disabled 重试，reasoning 可兜底）；客观题（单选/多选）**3 次采样投票**取多数；提示词改为**直接输出选项字母**（不再要求复述选项内容），并删掉"多选必须选两个以上"的凑数约束；解析兼容 JSON/代码块/"答案：X"/纯文本 | 用户反馈 V4.1 flash 上线后答题错误率高、AI实践经常不达标。实测模型本身正常，问题在：强关 thinking 降低推理质量、"必须选两个以上"会逼模型凑答案、选项内容→字母的匹配会失败退化成随机 | 2026-09-21 |
| D35 | 时长类文档任务点：真实打点 600 秒 + readEnd 后平台仍不计入（两次真机验证）。策略：**不假装完成**，本地记录尝试（24 小时内不重复消耗真实阅读时间），任务状态仍只认平台复查；"读到底"（completeRead）类文档可正常完成 | 实测证据：泰康/吉利 600/600 秒打点 + readEnd → isFinish 仍为 false；中国平安（completeRead）与科大讯飞（5 分钟）已完成。缺的是阅读器 JS 的页面/滚动事件参与计账，暂不引入伪造手段 | 2026-09-21 |
| D36 | 诚实性修复：直播强制 1 倍速真实时间并响应中断、失败必须返回失败；没有题库的章节测验返回 ERROR（不再记完成）；任务中心阶段被终止时不得走到"全部完成"；向导确认页回车＝取消；作业/文档提交写本地台账去重；tqdm 全局补丁加 try/finally 兜底 | 独立审计发现的 3 处"假完成"和 2 处误报，直接违反 AGENTS 铁律 1/2 | 2026-09-21 |
| D37 | 显示与日志减负：控制台每阶段只打一次 q 键提示；章节进度行压到 ~80 列；AI 写作器的重试/降级、逐题作答明细、随机作答降为 DEBUG（只进文件）；长视频/长文档开始时给一行用户可见进度；失败汇报压成一行汇总；日志文件默认 DEBUG（`CX_LOG_LEVEL=TRACE` 开全量） | 用户读日志反馈：全流程显示冗余、重复、看不懂；一次运行 5.6 万行 TRACE 大部分是逐心跳/逐题噪音 | 2026-09-21 |
| D38 | 按独立审计修掉 3 条"可能假完成"路径：①未知卡片类型不再静默丢弃（decode 收集 unknownCardTypes → get_job_list 按读取失败处理，不再走"空章节已完成"）；②章节文档不再只看 HTTP 200（校验 result=false，成功补 INFO 日志）；③章节测验"只保存未提交"与"提交后连续 3 次读不到成绩"改为 ERROR。另：study_read 校验业务返回、AI 实践失败日志带最终地址 | 独立审计报告（2026-09-21）指出这些路径会把未完成记成完成，直接违反铁律 1 | 2026-09-21 |
| D39 | 新增"复核环节"：AI 生成并提交的实质性文字（测验简答/作业简答/讨论回复/AI实践作答）统一留痕到 `~/.chaoxing/reviews/`（每天一个 Markdown + `index.jsonl`），提供 `./cx review`（今天/最近 N 天/全部/--list）交互式翻阅；每次刷完给一行提示。同时控制台整体减负：并发默认 4→2（降低验证码/403 概率）、去掉重复行、403 与验证码重试降噪、通知未配置降为 DEBUG | 用户反馈"整个页面杂乱、不清晰"，并要求增加可查阅 AI 生成内容的复核环节（留痕 + 明确查阅形式） | 2026-09-21 |
| D40 | 作答留痕改为"运行中实时显示"：作业/章节测验逐题输出「序号 · 题型 · 题干提示 · 答案」（长文本截断并标字数），讨论回复按 60 列折行完整显示；同一行同时写控制台与普通运行日志（`api.display.emit`），不再需要事后生成额外日志；复核文件（D39）保留作为事后翻阅 | 用户澄清：希望答题过程中就能看到第一题选了什么、简答写了什么、讨论回了什么，而不是事后翻文件 | 2026-09-21 |
| D41 | ①刷课数量语义写进向导与 README：数字 = 本次刷多少个未完成任务点（`pending[:N]`），已完成的永远跳过；②新增"开始前扫描"（`api/scan.py`，默认开启、无勾选、只读）：每次开刷前打印章节/教学任务的已完成·待刷·未解锁·暂不支持，并提醒漏做的作业/讨论；③刷课范围新增第 4 项"只刷讨论"，`--only-discussion`，任务中心内只处理 planType=14 | 用户反馈：不清楚输入数字的含义（以为可能只检查前 N 节）、希望默认自动扫描漏刷、希望讨论可以作为并列选项 | 2026-09-21 |
| D42 | 讨论拆成两种模式并共用一条链路：模式 1 = 任务里的主题讨论（planType=14，自动按分组刷）；模式 2 = `cx discuss` 直接读讨论区帖子列表（GET /pc/topic/topiclist/{bbsid}/getTopicList），用户挑帖回复。TaskCenter.study_discussion 重构为调用新的 reply_topic(bbsid, uuid)，两种模式共用"读已有回复→生成→审计→提交→留痕"；已回复过的帖子不重复发。控制台过滤规则也改为：非刷课阶段只显示 INFO 以上，DEBUG/TRACE 只进日志文件 | 用户要求"进入讨论区后可以解析所有帖子并自己选择回复哪个" | 2026-09-21 |
| D43 | 向导按范围定制提问 + 讨论区流程重做：①"只刷讨论"不再问数量，改为说明讨论怎么刷；②范围页新增"讨论怎么刷"子选项（task=任务里的主题讨论自动 / board=讨论区挑帖），写入 `[common] discussion_mode`；③讨论区模式流程 = 只读统计帖子数 → 用户挑帖（1,3,5 / 1-3 / all）→ 逐条草稿 → y/n 确认 → 一条条发送（间隔 1.5s），已回复过自动跳过；④`TaskCenter.reply_topic` 拆成 `draft_reply` + `submit_reply`（模式 1 行为不变）；⑤所有 y/n 提示统一小写 `[y/n · 回车＝否/是 · q 退出]`；⑥非交互输入反复无效时按默认继续，避免死循环 | 用户反馈：只刷讨论却问"刷多少个教学任务"、讨论区流程太死板、Yes/No 大小写不统一 | 2026-09-21 |
| D44 | 全流程连贯性审计与修复：①确认页补"讨论怎么刷"一行；②运行头明确是"章节任务点 N 个"；③讨论区模式结束后不再谎报"教学任务都已刷完"，改为"讨论区模式已结束"，通知同步；④开始前扫描的提醒按所选模式改写（只刷讨论时不再说"作业会自动完成"，讨论区模式不再说"讨论会自动回复"），只刷讨论时不再提示"被分组锁着"；⑤任务中心汇总区分"只刷讨论"与"讨论走讨论区模式"；⑥向导退出提示与运行时提示措辞统一；⑦**输入健壮性**：新增 `ask_choice()`，所有选择题（范围/讨论模式/选课/账号/删除/数量/结束后菜单）统一"重试上限 5 次 + 默认值/干净退出"，`ask()` 空输入也有上限，非交互输入（管道/重定向）不再死循环 | 用户要求"全流程衔接好、自然简洁、符合逻辑"；审计中发现 6 处衔接/措辞问题 + 1 类系统性死循环风险 | 2026-09-21 |
| D45 | 独立审计发现的三处硬伤修复 + 零基础文案打磨：①**只刷讨论走不通**（build_config 把 task_center 写成 false → 启动检查 hard_stop）→ build_config 在 only_discussion 时写 true，main 里同时强制打开；②**skip_discussion 未赋值**导致任务中心阶段每次 NameError 被当成"读取失败"→ 在 main 里定义 `skip_discussion = board_mode`；③答题方式表解包顺序错（need_ai 拿到说明文字）→ 题库模式被追问 AI Key、且在 Key 步骤选"不做测验"会丢掉刚填的 token，已修正为保留题库；④扫描行单位混用（"教学任务 9 个 · 已完成 62"）→ 改成"教学任务 9 个 · 任务点 77 个（已完成 62 · 待完成 9 · 未解锁 6）"；⑤讨论区帖子数被 10 页上限截断时说"共 200 条"→ 改为"至少 200 条（只读了前 10 页）"，并支持发完一批后继续挑 | 独立审计 Agent + 我的零基础视角审计；用户要求"0 基础也能看懂、逻辑顺畅" | 2026-09-21 |
| D46 | 全流程审计报告（独立 Agent）的 P1/P2 修复：①讨论区失败或一条没发时不再谎报"已发送"——`discuss_cli` 返回 `{ok, sent, skipped, reason}`，控制台/通知按真实条数汇报；②只刷讨论时不再出现"只刷任务中心/继续检查任务中心"的话术；③`--yes` 不再让讨论区静默群发（AI 回复发到公开讨论区必须逐条确认）；④讨论确认默认改成"回车＝跳过"，只有明确 y 才发送；⑤用户选的课没有讨论区时先问一句再换，不再静默换课；⑥范围4 的"讨论怎么刷"标题不再出现两次、确认页同一选择不再说三遍；⑦术语统一（评论区→讨论区、"按课程要求的顺序"、"讨论区（自己挑帖子）"）；⑧扫描报告补课程名、任务中心汇总折行到 76 列、讨论区预览行截断；⑨没有教学任务时说"没有找到教学任务"而不是"都已刷完"；⑩第一次运行会问一次可选的通知设置 | 独立审计 Agent 的全流程连贯性报告 + 我自己的零基础视角复核 | 2026-09-21 |
